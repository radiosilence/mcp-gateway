apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fastmail-mcp
resources:
  - namespace.yaml
  - postgres.yaml
  - hydra.yaml
  - service.yaml
  - ingress.yaml
  # secrets.example.yaml is a template — real secrets come from the cluster
  # secret manager, not kustomize.
